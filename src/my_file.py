from typing import Iterable
from abc import ABC

my_global_var = 3
a, b = 10


def sum_even_numbers(numbers: Iterable[int], self, aaa) -> int:
    (
        """Given an iterab
    return """
        """
    le of integers, return the sum of all even numbers in the iterable."""
    )
    # my_local_var = 10
    self.a = 10
    self.c
    self.b = 3

    return sum(num for num in numbers if (num % 2 == 0))


x = 10


class MyClass(ABC):
    z = 10

    def do_thing():
        pass
